datasource db {
    provider = "mongodb"
    url      = env("DATABASE_URL")
}

model Post {
    id          String @id @default(auto()) @map("_id") @db.ObjectId
    Username    String 
    PostMessage String
    Title       String
    Tags        String[]
    CreatedAt   String
}
